
    //i know i am supposed to use these some how but not sure what to do here...
    //need to make the crystals buttons, make them click, and then mnake them add numbers to the score,
    //also need them crystal buttons to generate numbers when game starts
    //need to have score add them up
    //have the win and loses add up and total
    //







Math.random



function getRndInteger(min, max) {
    return Math.floor(Math.random() * (max - min)) + min;